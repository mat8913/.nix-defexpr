diff --git a/ranger/gui/ui.py b/ranger/gui/ui.py
index 7adb0f1ee9..d44b3db99d 100644
--- a/ranger/gui/ui.py
+++ b/ranger/gui/ui.py
@@ -128,9 +128,10 @@ def initialize(self):
             self.is_set_up = True
             self.setup()
             self.win.addstr("loading...")
-            self.win.refresh()
             self._draw_title = curses.tigetflag('hs')  # has_status_line
 
+        self.win.refresh()
+
         self.update_size()
         self.is_on = True
 
@@ -151,10 +152,6 @@ def suspend(self):
         self.win.keypad(0)
         curses.nocbreak()
         curses.echo()
-        try:
-            curses.curs_set(1)
-        except curses.error:
-            pass
         if self.settings.mouse_enabled:
             _setup_mouse({"value": False})
         curses.endwin()
